spring:
  application:
    name: mc-acc
  profiles:
    active: DEV

  datasource:
    url: fix
    username: fix
    password: fix
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:/db.changelog/db.changelog-master.xml

server:
  port: 8081

---